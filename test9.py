#prompt user for number of students, enter all scores
#get best score
#assign grades based on score
#lists, if, while
#display ouput student number, score and grade
name = eval(input(""))
score = int(input(""))
Grade =  int(input(""))